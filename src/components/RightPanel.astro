---
interface Props {
	rawMarkdown?: string;
	filename?: string;
}
const { rawMarkdown = '', filename = 'content.md' } = Astro.props;
---

<aside
	id="rightpanel"
	class="rightpanel hidden lg:hidden xl:flex shrink-0 w-80 flex-col bg-bg-elevated border-l border-border-soft"
	aria-label="Raw markdown source"
	transition:persist
>
	<div class="flex items-center justify-between px-4 h-12 border-b border-border-soft shrink-0">
		<div class="flex items-center gap-2 text-sm min-w-0">
			<svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="text-fg-muted shrink-0" aria-hidden="true">
				<path d="M3 2h6l3 3v7a1 1 0 0 1-1 1H3a1 1 0 0 1-1-1V3a1 1 0 0 1 1-1ZM9 2v3h3" />
			</svg>
			<span class="font-mono text-xs text-fg-muted truncate">{filename}</span>
		</div>
		<button
			type="button"
			id="rightpanel-close"
			class="p-1 rounded text-fg-muted hover:bg-surface hover:text-fg border-0 bg-transparent cursor-pointer"
			aria-label="Close markdown source panel"
		>
			<svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
				<path d="M3 3l8 8M11 3l-8 8" />
			</svg>
		</button>
	</div>

	<pre class="flex-1 overflow-auto p-4 m-0 text-xs leading-relaxed font-mono text-fg-muted whitespace-pre-wrap break-words bg-bg-elevated">{rawMarkdown}</pre>
</aside>
